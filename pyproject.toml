[project]
name = "pathogen-daily-intelligence"
version = "1.3.0"
description = "GitHub Actions + Streamlit Multi-model, deep-content, evidence-audited bilingual pathogen daily intelligence system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
